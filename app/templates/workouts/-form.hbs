<form {{action 'save' on='submit'}}>
  <h2>{{errorMessage}}</h2>
  <div class="form-group">
    <label for="trained_at">Training date</label>
    {{input value=model.trained_at class='form-control'}}
  </div>
  <div class="form-group">
    <label for="day">Training day</label>
    {{input value=model.day class='form-control'}}
  </div>
  <div class="form-group">
    <label for="description">Description</label>
    {{input value=model.description class='form-control'}}
  </div>

  <button type="submit" class="btn btn-primary">Submit</button>
  <button {{action 'cancel'}} class="btn btn-default">Cancel</button>
</form>
